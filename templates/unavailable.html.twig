<!DOCTYPE html>
<html lang="{{ app_lang ?? 'en' }}">
  <head>
    <meta charset="UTF-8" />
    <title>UBarbershop – Admin Unavailable Days</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../public/css/admin.css" />
  </head>

  <body>
    <header class="site-header">
      <a class="logo logo-link" href="{{base_path}}" aria-label="UBarbershop Home">
        <img src="../public/images/logo.png" class="logo-img" />
        <span class="logo-text">UBarbershop</span>
      </a>

      <nav class="main-nav">
        <a href="{{base_path}}/callendar">{{ trans('nav.calendar') }}</a>
        <a href="{{base_path}}/admin">{{ trans('nav.addEmployee') }}</a>
        <a href="{{base_path}}/unavailable">{{ trans('nav.unavailable') }}</a>
        <a href="{{base_path}}/services">{{ trans('nav.services') }}</a>
        <a href="{{base_path}}">{{ trans('nav.exitAdmin') }}</a>
      </nav>

      <div class="header-actions">
        <div class="lang-switcher">
          <button data-lang="en">EN</button>
          <button data-lang="fr">FR</button>
        </div>
      </div>
    </header>

    <main class="page-content admin-page">
      <h1>{{ trans('unavailable.title') }}</h1>

      <div class="admin-card">
        <p class="admin-subtitle">{{ trans('unavailable.subtitle') }}</p>

        <div class="unavail-tip">
          {{ trans('unavailable.tip') }}
        </div>

        <div id="unavail-msg" aria-live="polite"></div>
        <div id="unavail-error" aria-live="polite"></div>

        <form id="unavail-form" novalidate>
          <!-- Date range -->
          <div class="range-grid">
            <div class="field">
              <label for="unavail-start">
                {{ trans('unavailable.startDate') }}
              </label>
              <input id="unavail-start" type="date" required />
            </div>

            <div class="range-arrow" aria-hidden="true">→</div>

            <div class="field">
              <label for="unavail-end">
                {{ trans('unavailable.endDate') }}
              </label>
              <input id="unavail-end" type="date" required />
            </div>
          </div>

          <div
            id="unavail-preview"
            class="unavail-preview"
            aria-live="polite"
          ></div>

          <label for="unavail-employee">
            {{ trans('unavailable.employee') }}
          </label>
          <select id="unavail-employee">
            <option value="all">
              {{ trans('unavailable.all') }}
            </option>
          </select>

          <label for="unavail-reason">
            {{ trans('unavailable.reason') }}
          </label>
          <input
            id="unavail-reason"
            type="text"
            data-i18n-placeholder="unavailable.reasonPlaceholder"
            placeholder="Vacation / Closed"
          />

          <div class="admin-actions">
            <button
              class="btn primary"
              type="submit"
            >
              {{ trans('unavailable.add') }}
            </button>
          </div>
        </form>

        <div class="employee-list-wrap" style="margin-top: 1.25rem;">
          <h2
            class="employee-list-title"
          >
            {{ trans('unavailable.listTitle') }}
          </h2>
          <div id="unavail-list"></div>
        </div>
      </div>
    </main>

    <footer class="site-footer">
      <p class="footer-bottom">
        &copy;
        <script>
          document.write(new Date().getFullYear());
        </script>
        UBarbershop
      </p>
    </footer>

    <script src="../public/js/lang.js"></script>
    <script type="module" src="../public/js/unavailable.js"></script>

    <!-- Gate admin pages behind login -->
    <script type="module">
      import { auth } from "../js/firebase.js";
      import { onAuthStateChanged } from "https://www.gstatic.com/firebasejs/12.9.0/firebase-auth.js";

      onAuthStateChanged(auth, (user) => {
        if (!user) {
          window.location.href = "login.html.twig";
        }
      });
    </script>

    <script src="../public/js/hamburgerMenu.js"></script>
    <script src="../public/js/activeNav.js"></script>
  </body>
</html>